[tool.poetry]
name = "my-tube-video-upload-service"
version = "0.1.0"
description = ""
authors = ["Bohdan Butko"]

[tool.poetry.dependencies]
python = "^3.9"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
